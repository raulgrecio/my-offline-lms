---
interface ProgressBarProps {
  progress: number;
}

const { progress } = Astro.props as ProgressBarProps;
---

<div class="progress-bar">
  <div class="progress-bar-fill" style={`width: ${progress}%`}></div>
</div>